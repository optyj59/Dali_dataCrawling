# 주간 업무 보고

**기간:** 2025년 11월 3일 - 2025년 11월 7일

## I. 금주 진행 상황

### 1. 프로젝트 초기 설정 및 설계
- **프로젝트 생성:** '유튜브 댓글 DWH 구축'을 위한 프로젝트 폴더 구조를 생성했습니다. (`src`, `data`, `logs` 등)
- **아키텍처 설계:** Playwright와 BeautifulSoup을 이용한 크롤링 엔진, PostgreSQL을 이용한 데이터베이스 관리 등 핵심 기술 스택과 데이터 처리 파이프라인을 설계했습니다.
- **데이터베이스 스키마 정의:** `database_schema.md` 파일을 통해 SCD Type 2 이력 관리를 포함한 6개의 테이블 구조를 상세히 설계했습니다.

### 2. 크롤링 엔진 개발 및 개선
- **핵심 기능 구현:** `crawler_engine.py`에 키워드 기반 영상 검색, 메타데이터 추출, 댓글 수집 등 핵심 크롤링 로직을 구현했습니다.
- **댓글 수집 로직 고도화:**
    - 기존의 고정 시간 대기(`sleep`) 방식에서 발생하던 불안정성 문제를 해결하기 위해, Playwright의 `wait_for_load_state('networkidle')`을 사용하는 동적 대기 방식으로 변경하여 모든 댓글을 안정적으로 로드하도록 개선했습니다.
    - 디버깅 과정에서 댓글 섹션을 식별하는 CSS 선택자(`ytd-comments-header-renderer`)가 유효하지 않은 문제를 발견하고, 실제 댓글 스레드를 가리키는 `ytd-comment-thread-renderer`로 교체하여 로딩 성공률을 높였습니다.

### 3. 댓글 수집 로직 안정화 및 고도화
- **문제 진단:** 기존 크롤링 로직이 유튜브 댓글의 비동기적 로딩(Asynchronous Loading) 특성 때문에 실패하는 것을 확인했습니다. 스크립트가 댓글 내용을 읽으려는 시점에 실제 데이터가 아직 DOM에 렌더링되지 않은 '뼈대(Skeleton)' 상태임을 파악했습니다.
- **1단계: 2단계 대기 전략 도입:**
    - **내용:** 댓글 전체를 감싸는 컨테이너(`ytd-comments#comments`)가 나타난 후, 그 안에서 실제 첫 댓글(`ytd-comment-thread-renderer`)이 렌더링될 때까지 순차적으로 기다리는 2단계 대기 로직을 구현했습니다.
    - **결과:** 이 방식으로 초기 댓글 묶음을 안정적으로 가져오는 데 성공했습니다.
- **2단계: 스크롤링 로직 개선:**
    - **내용:** 더 많은 댓글을 수집하기 위해 스크롤 기능을 추가했으나, `window.scrollTo`를 이용한 초기 시도는 스크롤 위치가 최상단으로 이동하는 문제로 실패했습니다.
    - **개선:** 페이지 전체가 아닌, '현재 로드된 마지막 댓글'을 기준으로 `scrollIntoView()`를 호출하는 방식으로 변경하여 스크롤 문제를 해결했습니다.
- **3단계: 데이터 추출 및 저장 기능 구현:**
    - **내용:** 수집된 데이터를 `database_schema.md` 기반으로 `data/comments_raw.csv` 파일에 저장하는 기능을 구현했습니다.
    - **개선:** 저장 과정에서 발생한 두 가지 문제를 해결했습니다.
        1.  `data` 디렉토리가 없을 때 발생하는 `FileNotFoundError`를 방지하기 위해, 파일 저장 전 `os.makedirs`로 디렉토리를 자동 생성하는 로직을 추가했습니다.
        2.  댓글 내용의 개행 문자로 인해 CSV 파일 구조가 깨지는 문제를 `csv.DictWriter`의 `quoting=csv.QUOTE_MINIMAL` 옵션으로 해결했습니다.

### 4. 성능 개선을 위한 병렬 처리 리서치
- **필요성:** 다수의 영상 댓글을 순차적으로 수집할 경우 전체 작업 시간이 길어지는 문제를 해결하기 위해, 크롤러 병렬 처리 방안을 리서치했습니다.
- **방법론 조사:**
    1.  **멀티프로세싱 (Multiprocessing):** 여러 CPU 코어를 활용하여 다수의 크롤러 프로세스를 동시에 실행하는 방식. 각 프로세스가 독립적인 브라우저를 가지므로 가장 확실한 성능 향상을 기대할 수 있으나, 메모리 사용량이 높습니다.
    2.  **비동기 동시성 (Asyncio):** 하나의 프로세스 내에서 여러 브라우저 탭(Page)을 동시에 열어 처리하는 방식. 메모리 사용은 적지만, 단일 CPU 코어 내에서만 동작하여 성능 향상에 한계가 있을 수 있습니다.
- **결론:** 현재 프로젝트와 같이 브라우저 자체의 부하가 큰 작업에는 **멀티프로세싱** 방식이 가장 적합할 것으로 판단됩니다.

## II. 금주 목표
- **댓글 수집 기능 안정화:** 개선된 크롤링 엔진을 최종 테스트하고, 댓글 수와 무관하게 목표 영상의 모든 댓글을 빠짐없이 수집하는 것을 이번 주 최종 목표로 설정합니다. **(달성)**
    - **결과물:** 약 50여 개의 댓글(ID, 작성자, 내용, 좋아요 수)을 안정적으로 수집하여, 개행 문자까지 완벽히 처리된 CSV 파일로 저장하는 기능 구현 완료.

## III. 다음 주 계획 (예정)
- **추가 정보 수집:** `database_schema.md`에 명시된 항목 중 아직 수집하지 않고 있는 `parent_comment_id`(대댓글 식별), `created_time`(댓글 작성 시간) 등의 CSS 선택자를 찾아 수집 기능을 확장합니다.
- **전체 댓글 수집 로직 구현:** 현재는 고정된 횟수만큼 스크롤하지만, 동적으로 페이지 끝까지 스크롤하여 모든 댓글을 수집하는 로직으로 고도화합니다.
- **데이터베이스 연동(`db_manager.py`) 개발 착수:** 수집된 CSV 데이터를 PostgreSQL에 적재하는 기능을 개발합니다.
